name: Daily Briefing

on:
  schedule:
    - cron: '0 13 * * 1-5'  # 9am ET Monday-Friday
  workflow_dispatch:          # Manual trigger available

jobs:
  briefing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install yfinance pandas numpy schwab-py python-dotenv requests beautifulsoup4

            - name: Run daily briefing
        env:
          CONGRESS_API_KEY: ${{ secrets.CONGRESS_API_KEY }}
        run: |
          cd screener
          python report.py
          python signal_history.py

      - name: Commit briefing and signal history
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add logs/briefing_*.md
          git add -f data/signal_history.json
          git diff --cached --quiet || git commit -m "Daily briefing and signal history $(date +%Y-%m-%d)"
          git push

      - name: Commit briefing
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add logs/briefing_*.md
          git diff --cached --quiet || git commit -m "Daily briefing $(date +%Y-%m-%d)"
          git push
